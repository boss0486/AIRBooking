@using WebCore.Entities;
@using WebCore.Services;
@using WebCore.Model.Enum;
@using WebCore.Model.Services
@{
    ViewBag.Title = Helper.Page.MetaSEO.MetaTitle;
}
<div class="row">
    <div class="col-md-6">
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    <label class="form-label">Quốc gia:</label>
                    <select id="ddlNational" name="ddlNational" class="form-control show-tick">
                        <option value="">-@Helper.Language.Resource.Label.Option-</option>
                        @Html.Raw(NationalService.DropdownList(""))
                    </select>
                    <span id="lblNational" class="message msg-box"></span>
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    <label class="form-label">Khu vực nội địa:</label>
                    <select id="ddlAreaInland" name="ddlAreaInland" class="form-control show-tick" data-live-search="true">
                        <option value="-">@Helper.Language.Resource.Label.Option</option>
                        @Html.Raw(AreaInlandService.DropdownList(string.Empty))
                    </select>
                    <span id="lblAreaInland" class="message msg-box"></span>
                </div>
            </div>
        </div>
        <div class="form-group">
            <label class="form-label">Tiêu đề</label>
            <input id="txtTitle" name="txtTitle" maxlength="50" type="text" class="form-control" value="" />
            <span id="lblTitle" class="message msg-box"></span>
        </div>

        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    <label class="form-label">Mã IATA</label>
                    <input id="txtIataCode" name="txtIataCode" maxlength="5" type="text" class="form-control" value="" />
                    <span id="lblIataCode" class="message msg-box"></span>
                </div>
            </div>
        </div>
        <div class="form-group">
            <label class="form-label">Mô tả</label>
            <textarea id="txtSummary" name="txtSummary" class="form-control"></textarea>
            <span id="lblSummary" class="message msg-box"></span>
        </div>
        <div class="form-group m-t-5">
            <input id="cbxActive" name="cbxActive" type="checkbox" class="filled-in" checked />
            <label for="cbxActive">Kích hoạt</label>
            <span id="lblActive" class="message msg-box"></span>
        </div>
        <div class="form-group text-right">
            <button id="btnReset" class="btn btn-danger" type="reset">@Helper.Language.Resource.Label.BtnReset</button>
            <button id="btnCreate" class="btn btn-primary" type="button">@Helper.Language.Resource.Label.BtnCreate</button>
        </div>

        @{
            if (PermissionService.CheckActionInMenuItem(Helper.Page.MetaSEO.ControllerText, WebCore.Entities.RoleActionKey.DataList))
            {
                <div class="form-group text-right att-data-list">
                    <a href="@Helper.Page.Navigate.PathDataList()">
                        @Helper.Language.Resource.Label.DataList <i class="fas fa-arrow-circle-right"></i>
                    </a>
                </div>
            }
        }
    </div>
</div>
@section scripts{
    <script src="~/Areas/Management/_script/app-flight.js"></script>
}

